\begin{frame}{MPI}
Rank 0 working as the orchestrator and rank 1 - N-1 as the workers. The orchestrator sends eahc worker a chunk of teh original two matrices 
each worker recieves multiplies and retuens the resulting chunk. 
\end{frame}